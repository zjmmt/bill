name: Repository checks

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  repository:
    runs-on: windows-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Validate documentation, architecture, security boundaries, and generated facts
        shell: cmd
        run: call scripts\check-repository.cmd
