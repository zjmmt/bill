# AGENTS.md

本文件是仓库地图，不是百科全书。先定位任务所属文档，再读取最小必要上下文。

## 从这里开始

1. 阅读 [README.md](README.md) 了解产品承诺。
2. 阅读 [ARCHITECTURE.md](ARCHITECTURE.md) 了解依赖方向与系统边界。
3. 从 [docs/index.md](docs/index.md) 进入具体产品、设计或运行文档。
4. 复杂工作必须先创建或更新 `docs/exec-plans/active/` 下的执行计划。

## 任务导航

- 产品范围、用户价值：`docs/PRODUCT_SENSE.md`、`docs/product-specs/`
- 交互与 Compose 页面：`docs/DESIGN.md`、`docs/FRONTEND.md`
- 领域模型、数据流：`docs/design-docs/`
- 支付宝/微信/银行接入：`docs/product-specs/source-coverage.md`、`docs/design-docs/ingestion-and-source-adapters.md`
- 去重、转账、还款、退款：`docs/design-docs/reconciliation.md`
- 安全、权限、备份：`docs/SECURITY.md`
- 可靠性、测试、可观测性：`docs/RELIABILITY.md`
- 计划、决策与债务：`docs/PLANS.md`、`docs/exec-plans/`、`docs/decisions/`
- 质量缺口：`docs/QUALITY_SCORE.md`

## 核心不变量

1. 默认无自有服务端；原始财务数据默认不离开设备。
2. 支付宝、微信支付、银行是同级来源，禁止把通用逻辑写成支付宝特例。
3. 来源能力可以不同，但每类来源必须有明确的采集、回退和验收路径。
4. `RawEvent` 不可变；重新解析产生新版本或派生记录，不覆盖证据。
5. 自动识别先生成 `Draft`；只有策略允许且达到阈值时才能自动确认。
6. 正式交易由平衡的 `Entry` 组成；渠道、资金账户和经济事件必须分离。
7. 转账、充值、还款、退款和投资申赎不得误算为普通收入或支出。
8. 外部数据只在边界解析和校验；未知格式必须失败为可诊断状态。
9. 日志、崩溃报告、测试夹具不得包含未脱敏财务数据、账号或完整通知。
10. 权限最小化；不得读取其他 App 私有目录，不把无障碍或短信权限当作默认主线。
11. 任何“已支持”声明都必须有脱敏样本、回放测试和用户可见回退路径。
12. 影响账务语义的变更必须同步设计文档、决策记录和迁移/测试说明。

## 推荐架构方向

业务流固定为：

`Capture -> RawEvent -> Parse/Normalize -> Draft -> Reconcile -> Transaction/Entries -> Review`

依赖只能向领域核心收敛：

`Android/UI -> Application -> Domain <- Data/Source Adapters`

来源适配器只负责采集、解析、标准化和来源证据，不负责直接记账。账务分类与关联由共享领域服务完成。

## 工作协议

开始前：

- 确认当前工作树状态，不覆盖用户已有变更。
- 找到对应规格与活跃计划；没有则先补最小计划。
- 将不确定事实写成假设或研究门，而不是写成确定能力。

实现时：

- 优先做纵向小切片，让单个来源样本能走完整条流水线。
- 支付宝、微信和银行共用契约；来源差异留在独立适配器。
- 错误必须包含可操作的修复提示，但不得回显敏感原文。
- 规则变更必须配套固定回放样本，且保留旧行为的迁移判断。

完成前：

- 运行 `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\check-docs.ps1`。
- 实现存在后，再运行仓库记录的格式化、静态检查、单元测试和 Android 测试命令。
- 更新执行计划的进度、决策和意外发现。
- 若真实行为改变，更新其唯一事实来源并修正指向它的索引。

## 文档规则

- 每份持久文档包含状态、所有者、最后核验日期和事实来源说明。
- 索引只做导航；细节只在一个地方维护，其他位置使用链接。
- `docs/generated/` 只能由生成器写入；无生成器时不得手工伪造“生成文档”。
- 完成的执行计划移到 `docs/exec-plans/completed/`，不得删除决策历史。
- 外部讨论中形成的约束必须回写仓库；仓库外知识对未来智能体等同于不存在。

## 需要人工判断的情况

- 新增高风险权限、云同步、遥测或第三方数据处理方。
- 改变本地优先承诺、加密/备份格式或数据删除语义。
- 在缺少真实脱敏样本时声明某银行、微信或支付宝格式已稳定支持。
- 自动合并低置信度交易，或改变转账/还款/投资的账务语义。

如果任务受阻，不要重复尝试同一提示；把缺失的样本、工具、文档、约束或反馈回路写入执行计划与技术债务清单。
