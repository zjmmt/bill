# ExecPlan 0014：采集入口可靠性收口

- 状态：已完成
- 所有者：项目维护者
- 最后核验：2026-08-02
- 事实来源：2026-08-02 真机实际使用反馈、采集与审核相关实现及测试

## 目的与用户可见结果

修复截图快捷采集、微信账单截图、通知监听状态和相册 OCR 导入四条真实使用路径。完成后，能够识别的内容必须进入待审核草稿；无法识别但已成功取得的图片必须建立可手工补录的空白待审核项；无法取得图片或权限异常必须给出明确反馈，不能静默失败。

## 范围与非目标

本计划包含截图及相册 OCR 的空结果回退、微信账单截图的可见反馈与草稿落地、通知监听授权后的连线状态恢复、不同图片连续导入时的证据标识冲突修复，以及覆盖真实持久化边界的回归测试。

本计划不扩展新的银行模板、不引入云端 OCR、不改变用户最终确认后才正式记账的规则，也不把识别失败内容自动确认为交易。

## 上下文与仓库导航

- 快捷截图入口：`app/src/main/kotlin/dev/bill/app/quickcapture/`
- 相册 OCR 入口：`app/src/main/kotlin/dev/bill/app/quickcapture/SelectedPhotoOcrImporter.kt`
- 通知监听：`app/src/main/kotlin/dev/bill/app/notification/`
- OCR 文本契约与解析：`source/generic-photo-ocr/`
- 证据生命周期与持久化：`application/`、`data/local/`
- 产品回退约束：`docs/product-specs/source-coverage.md`

业务流水线仍为 `Capture -> RawEvent -> Parse/Normalize -> Draft -> Reconcile -> Transaction/Entries -> Review`。空 OCR 只允许生成待审核项，不得绕过审核生成正式交易。

## 进度

- [x] 2026-08-02 重新核对四条用户实测故障及现有测试覆盖边界。
- [x] 2026-08-02 - 为四条故障补充单元回归，并新增 OCR→证据暂存→Room→待审核的仪器测试源码。
- [x] 2026-08-02 - 修复空 OCR 回退、图片暂存契约、通知连线竞态与截图结果回执。
- [x] 2026-08-02 - 目标单元测试、仪器测试编译以及完整 `test lint assembleDebug assembleRelease` 通过；ADB 检查时没有在线设备，仪器测试执行仍待补。
- [x] 2026-08-02 - 在港版三星 S24 Ultra 原地保留数据升级至内部签名 v4；冷启动成功，通知监听服务由系统实际绑定。
- [x] 2026-08-02 - 更新事实文档，按修复、连接反馈和文档三个提交拆分并推送；生成、验签并交付 arm64-v8a 正式 APK。

## 意外发现

- 现有 OCR 仪器测试只覆盖“位图识别后能否被解析”，没有经过 Room 证据生命周期和待审核草稿持久化，因此无法发现相册入口的全局冲突。
- 截图与相册入口都把 OCR 空结果提前转换为失败，虽然通用解析器本身已经支持 `NeedsUserReview(candidate = null)`，导致原本设计的手工补录回退没有真正接通。
- 通知服务连接回调再次查询系统授权；系统授权状态短暂滞后时会把已连接写成未连接，之后仅刷新授权无法恢复连接位。
- `RoomRawEventRepository` 的暂存消费集合漏掉 `PHOTO_OCR` 和 `SHARE_FILE`；生产入口已经建立租约并写入证据，Room 却把存在的租约当成冲突，因此重试新 UUID 也会稳定失败。这是相册“空库也冲突”和快捷截图无草稿的共同根因。
- 已安装的正式版与 Debug 仪器测试包签名不同，三星系统拒绝附着测试；测试包未运行且随后已卸载。为保护现有账本，本轮没有卸载正式版或改签测试包。
- 覆盖安装后通知监听授权仍在，系统服务记录显示 `requested=true`、`received=true`、`hasBound=true`；截图无障碍服务当时未授权，因此不把快捷截图入口写成已完成真机交互验收。

## 决策日志

- 2026-08-02 - 成功取得图片但 OCR 为空时，建立不含虚构金额的空白待审核项；用户明确要求识别不了时可以手动补录。
- 2026-08-02 - 相同内容的再次导入按“可能重复”交给审核，不把正常重复内容冒充证据标识冲突。
- 2026-08-02 - 真机验收是本计划完成门槛；组件测试通过不能再表述为实际入口可用。
- 2026-08-02 - 平台 listener 连接回调在连接存续期间是授权可用的强证据；enabled-listener 包查询只用于无连接时的授权判断，避免 OEM/系统状态传播延迟覆盖真实连接。
- 2026-08-02 - 项目负责人将内部签名 v4 APK 的完成、覆盖安装和路径交付定为本轮目标终点；未开启无障碍权限时不擅自修改系统授权，也不为跑仪器测试卸载正式版数据。

## 实施步骤

1. 为 OCR 文本契约定义可持久化的空识别结果，并让截图与相册入口共同使用。
2. 用真实 Room 仓库串联两次独立导入，定位并修复证据、原始事件或提交键的稳定标识冲突。
3. 将通知服务“已连接”与授权查询解耦，并在授权返回后提供一次受控的重新绑定恢复。
4. 为快捷截图保存不含财务内容的结果回执，确保即时提示丢失时仍可在应用内看到结果。
5. 运行单元测试、仪器测试、静态检查与构建，再在真机按用户步骤验证。

## 具体命令

工作目录：`C:\Users\zjmmt\Documents\Bill`

- 目标测试：`cmd.exe /d /s /c scripts\android.cmd testDebugUnitTest`
- 静态检查和构建：`cmd.exe /d /s /c scripts\android.cmd lint assembleDebug assembleRelease`
- 文档检查：`cmd.exe /d /s /c powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\check-docs.ps1`
- 真机检查与安装：使用仓库内 Android SDK 的 `adb.exe`，只在本地构建通过后执行。

## 验证与验收

- 无关页或无可识别文字的截图：出现明确反馈，并建立一个可编辑、金额为空的待审核项；不产生正式交易。
- 微信账单截图：无论识别成功或进入空白回退，都有用户可见结果；识别出的金额进入待审核草稿。
- 通知权限：授权后服务真实连接时状态为“已就绪”；授权回调竞态不会永久停留在“尚未连线”。
- 相册选择：空数据库连续选择不同图片不出现“与已有记录冲突”；相同内容再次选择只进入可能重复审核路径。
- 自动化测试必须穿过 OCR、证据生命周期、Room 和待审核持久化边界；最终仍需目标真机逐项确认。
- 测试日志和夹具不得包含真实账号、完整通知或用户截图。

## 幂等、回滚与恢复

所有导入继续使用不可变 `RawEvent` 和独立命令标识。中断后可重新选择图片或重新截图；重复内容不会覆盖旧证据。若新回退逻辑异常，可按入口回滚，不需要迁移既有正式交易。

## 结果与复盘

代码已接通空 OCR 手工补录、图片证据暂存事务、相同内容可能重复提示、通知授权后受控 rebind 和不含财务内容的截图结果回执。JVM/Android 单元测试、lint、Debug/Release 构建与新增仪器测试源码编译通过。

内部签名 arm64-v8a 正式包为 `app/build/outputs/apk/release/app-arm64-v8a-release.apk`，版本为 `4 / 0.1.0-internal.4`，使用 v2 APK 签名，证书主题为 `CN=Bill Internal Test, O=Bill`。该包已在 `SM-S9280` 上用 `adb install -r` 从 v3 原地升级成功，保留应用数据；主 Activity 启动成功，进程存活，通知监听服务得到系统绑定。

真机旁装的 Debug 测试包因签名与正式版不同被系统拒绝附着，随后已卸载；截图无障碍服务当时也未授权。本结果因此不虚构“真机四入口逐项交互全部通过”，而以自动化回归、正式包安装/启动和通知系统绑定作为本轮交付证据。快捷截图与真实相册选择仍由测试用户在开启相应权限后的实际使用反馈继续验证。
